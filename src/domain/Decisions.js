class Decisions {
    // 당첨 로또 번호와 그냥 로또 번호를 비교하는 함수
    howManyCorrect(targetLotto, lotto) {

        return [correctNum, price]
    }
    caculateProfit(targetLotto, lotto) {
        const num = lotto.compareLotto(targetLotto)
        // num을 토대로 이율을 계산하는 로직을 구현한다.
        
    }
}

export default Decisions